# === SECURITY ===
FLASK_SECRET_KEY=change-this-to-a-very-secure-random-string

# === UI CUSTOMIZATION (WHITE LABEL) ===
# App Name appearing in Header and Title
APP_TITLE=DevOps Tools Hub
# Short description on the landing page
APP_DESCRIPTION=Centralized platform for automation, monitoring, security, and developer utilities.
# URL or Path to App Logo (can be external URL or local path in /static/...)
APP_LOGO=/static/images/logo.png
# URL or Path to Favicon
APP_FAVICON=/static/images/favicon.png

# === LOGGING ===
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# JSON log format (suitable for Docker/ELK) or Plain Text (false)
USE_JSON_LOG=true

# === SONARQUBE CONFIGURATION (REQUIRED FOR SCANNER) ===
# SonarQube Server URL
SONAR_HOST_URL=https://sonarqube.example.com
# SonarQube User Token (Generate in User > Security)
SONAR_LOGIN_TOKEN=sqp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# (Optional) Default exclusions
SONAR_EXCLUSIONS=**/*_test.go,**/mocks/**,**/*.html

# === SONAR SCANNER PERFORMANCE ===
SCANNER_HEAP_MIN=-Xms512m
SCANNER_HEAP_LIMIT=-Xmx2048m
CPU_NICE_ADJUSTMENT=10
# Allowed CPU cores (example: 0,1,2,3)
# CPU_AFFINITY=0,1

# === INTEGRATIONS (OPTIONAL) ===
# If you have a Stirling PDF URL
STIRLING_STUDIO_URL=https://pdf.example.com/
# Simple password for GitHub Access Check page (leave empty if not used)
GITHUB_ACCESS_PASSWORD=secret
# GitHub Personal Access Token (PAT) - Optional alternative to using ~/.netrc
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
